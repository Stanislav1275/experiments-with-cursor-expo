{
  "name": "experiments-with-cursor-expo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo with download-kit package and example app flows.",
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "typecheck": "npm run typecheck -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
